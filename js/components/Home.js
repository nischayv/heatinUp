import React, {Component} from 'react';
import {
    StyleSheet,
    Text,
    View
} from 'react-native';

const Home = () => {
    return (
        <View style={{flex: 1}}>
           <Text>You are now being tracked by the NSA!</Text>
        </View>
    );
};

export default Home;